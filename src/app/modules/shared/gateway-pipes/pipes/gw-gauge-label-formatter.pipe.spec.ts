import { GwGaugeLabelFormatterPipe } from './gw-gauge-label-formatter.pipe';

fdescribe('GwGaugeLabelFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new GwGaugeLabelFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
