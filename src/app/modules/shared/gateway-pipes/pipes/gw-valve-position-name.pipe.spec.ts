import { GwValvePositionNamePipe } from './gw-valve-position-name.pipe';

fdescribe('GwValvePositionNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GwValvePositionNamePipe();
    expect(pipe).toBeTruthy();
  });
});
