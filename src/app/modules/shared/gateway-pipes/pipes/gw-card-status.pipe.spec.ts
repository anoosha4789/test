import { GwCardStatusPipe } from './gw-card-status.pipe';

fdescribe('GwCardStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new GwCardStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
