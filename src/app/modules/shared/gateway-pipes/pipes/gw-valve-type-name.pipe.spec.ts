import { GwValveTypeNamePipe } from './gw-valve-type-name.pipe';

fdescribe('GwValveTypeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GwValveTypeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
