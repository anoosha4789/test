import { GwTruncatePipe } from './gw-truncate.pipe';

fdescribe('GwTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new GwTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
