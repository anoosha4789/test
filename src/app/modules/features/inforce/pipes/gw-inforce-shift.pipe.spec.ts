import { GwInforceShiftPipe } from './gw-inforce-shift.pipe';

fdescribe('GwInforceShiftPipe', () => {
  it('create an instance', () => {
    const pipe = new GwInforceShiftPipe();
    expect(pipe).toBeTruthy();
  });
});
